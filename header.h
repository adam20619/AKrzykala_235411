#include <vector>
#ifndef header
#define header

void Show_Solution(std::vector<double>solution);
void merge_sort(int tablica[],int start, long int koniec);
void quicksort(int tablica[], long int p, long int r,int coeff);
bool isSorted(int a[], long int end);
void introspective_Sort(int *tab, long int size);
void TEST(void);

#endif
